import issue20260901 from "../data/issues/2026-09-01.json";
import issue20260831 from "../data/issues/2026-08-31.json";

type Citation={label:string;url:string};
type Item={title:string;url:string;tag:string;source?:string;sourceDate?:string;takeaway:string;problem:string;why:string;existing:string;solution:string;results:string;assessment:string;citations?:Citation[]};
type Issue={date:string;label:string;summary:string[];items:Item[]};
type IssueRecord={date:string;label:string;summary:string[];items:Array<{canonical_url:string;title:string;venue_source:string;date:string;tag:string;one_sentence_takeaway:string;problem:string;why_it_matters:string;existing_approaches:string;limitations:string;proposed_solution:string;key_differences:string;results_with_conditions:string;assessment:string;citations:Citation[];content_hash:string}>};
function issueFromRecord(issue:IssueRecord):Issue{return {date:issue.date,label:issue.label,summary:issue.summary,items:issue.items.map(item=>({title:item.title,url:item.canonical_url,tag:item.tag,source:item.venue_source,sourceDate:item.date,takeaway:item.one_sentence_takeaway,problem:item.problem,why:item.why_it_matters,existing:`${item.existing_approaches} Limitation: ${item.limitations}`,solution:`${item.proposed_solution} Key difference: ${item.key_differences}`,results:item.results_with_conditions,assessment:item.assessment,citations:item.citations}))}}
const issues:Issue[]=[issueFromRecord(issue20260901 as IssueRecord),issueFromRecord(issue20260831 as IssueRecord),{date:"2026-08-30",label:"30 Aug 2026",summary:[
"Fixed per-token MoE routing work is still material at decode batch sizes: vLLM removes eleven redundant GPU kernels per GPT-OSS MoE layer and reports a 5.6% throughput gain at concurrency 64.",
"CUDA-graph coverage is becoming phase-aware: Megatron now uses logarithmic spacing for wide prefill ranges and linear spacing for tightly clustered decode batches.",
"Inference portability moved on two fronts: llama.cpp improves Adreno prefill dispatch, while vLLM expands disaggregated MLA execution across DCP layouts.",
"Correct metadata is the recurring systems theme: DCP KV ownership, speculative draft sequence lengths, and host-registration boundaries all need layout-aware treatment rather than generic bookkeeping.",
"Four requested papers extend the same arc from quantization and lossless KV merging to high-concurrency speculation and production sparse prefill."
],items:[
{title:"vLLM: Reuse SparseMatrix routing metadata in GPT-OSS MoE",url:"https://github.com/vllm-project/vllm/pull/45457",tag:"MoE · vLLM",source:"vLLM pull request #45457 · merged",sourceDate:"2026-08-29",takeaway:"Reusing the routing metadata already produced by Triton top-k removes eleven redundant GPU kernels per MoE layer and improves measured GPT-OSS serving throughput by 5.6% at concurrency 64.",problem:"On the Triton Kernels 3.6+ GPT-OSS path, top-k already creates a SparseMatrix with dispatch indices and expert histograms, but vLLM discarded it, repacked the bitmatrix, and rebuilt the same metadata for every MoE layer and decode step.",why:"GPT-OSS-20B has 24 MoE layers, so roughly 200 microseconds of fixed routing work per layer can dominate low-batch decode and remains inside captured CUDA graphs even when CPU launch overhead is removed.",existing:"The previous no-expert-map path reconstructed routing metadata from top-k IDs. That was necessary only when an expert map changed the IDs; without remapping it duplicated bitmatrix packing, sorting, and histogram kernels.",solution:"The merged path constructs RoutingData, GatherIndx, and ScatterIndx directly from the SparseMatrix returned by top-k. Expert-map and legacy Triton 3.5.1 paths remain unchanged.",results:"RTX 5090, Triton Kernels 3.6: routing construction fell from 458.5–563.9 µs to 260.3–321.4 µs across 1–4096 tokens, with CUDA kernels reduced 20→9. H800, GPT-OSS-20B, random 128-token input / 256-token output: concurrency 64 output throughput 5,344→5,643 tok/s (+5.6%) and mean TPOT 11.63→11.00 ms (-5.4%); single-stream CUDA-graph TPOT 5.06→4.52 ms (-10.7%) and output throughput +11.8%. GSM8K 200-sample accuracy was identical in the reported run.",assessment:"High-value, well-contextualized optimization. The gain is strongest when fixed routing overhead is a meaningful fraction of each step and depends on the Triton Kernels 3.6+ path; expert-mapped and bundled legacy paths do not benefit. Worth reproducing for GPT-OSS MoE serving.",citations:[{label:"Merged PR and benchmark tables",url:"https://github.com/vllm-project/vllm/pull/45457"}]},
{title:"Megatron-LM: Hybrid CUDA-graph sizing for prefill and decode",url:"https://github.com/NVIDIA/Megatron-LM/pull/6667",tag:"CUDA Graphs · Megatron",source:"Megatron-LM pull request #6667 · merged",sourceDate:"2026-08-29",takeaway:"Megatron now spaces CUDA-graph sizes logarithmically for prefill and linearly for decode, reducing padding around common decode concurrency without exploding the number of prefill graphs.",problem:"One global CUDA-graph size distribution cannot fit both prefill token counts, which span orders of magnitude, and decode counts, which cluster near the active request count.",why:"A request shape that misses a captured size is padded upward; coarse decode spacing wastes GPU work, while dense prefill spacing consumes capture time and memory through too many graphs.",existing:"Exponential spacing bounded prefill padding with few graphs but created coarse decode buckets; linear spacing improved decode coverage but would require many graphs across the full prefill range.",solution:"A new HYBRID default resolves to exponential sizing for prefill/mixed graphs and linear sizing for decode-only graphs while keeping the previous EXPONENTIAL and LINEAR modes available.",results:"For the documented max-requests=40 example, decode graph sizes change from [40,16,8] to [40,32,24,16,8]. Mean padding over sizes 8–40 falls from 1.44× to 1.18× and worst-case padding from 2.35× to 1.78×, at the cost of two additional decode graphs. Applying linear spacing globally would expand the prefill set from 9 to 144 graphs. No end-to-end throughput or latency result was reported in the primary source.",assessment:"A clean scheduling/configuration improvement with a persuasive shape-level analysis. It is now the default, so operators should watch graph capture memory and verify workload-specific latency; the absence of end-to-end data limits the strength of the performance claim. Worth reading for CUDA-graph policy design.",citations:[{label:"Merged PR and sizing analysis",url:"https://github.com/NVIDIA/Megatron-LM/pull/6667"}]},
{title:"llama.cpp: Better OpenCL matmul dispatch for Adreno X2E and A7X",url:"https://github.com/ggml-org/llama.cpp/pull/27640",tag:"Edge GPU · llama.cpp",source:"llama.cpp pull request #27640 · merged",sourceDate:"2026-08-29",takeaway:"Hardware-gated dispatch changes avoid two badly matched Adreno GEMM paths, with author-reported prefill gains of roughly 20–25% on GPT-OSS-20B/X2-90 and 9% on Gemma-3n/Adreno 740.",problem:"Two Adreno generations selected matmul kernels that their compilers or microarchitectures handled poorly: an opt-in F16×F32 xmem path stayed disabled on X2E, while A7X spilled heavily in a tiled F32 GEMM.",why:"A single mis-dispatched projection kernel can dominate prompt processing on edge SoCs; the GPT-OSS-20B profile spent 40.8% of prefill GPU time in the affected F16×F32 kernel.",existing:"The faster xmem path already existed but was opt-in, and the tiled F32 path was selected broadly despite A7X compiler register spilling. Moving the whole operation to CPU was tested but harmed decode through per-layer transfers.",solution:"Enable xmem F16×F32 GEMM by default only on X2E, and route batched F32×F32 around the tiled kernel only on A7X while retaining per-row GPU execution and explicit environment overrides.",results:"Author measurements report about 20% prefill improvement in the overview and 25% in the commit detail for GPT-OSS-20B on Adreno X2-90; the affected kernel previously took 40.8% of prefill time. On Gemma-3n-E4B / Adreno 740, bypassing the tiled F32 path improved prefill by about 9%. Decode is unchanged by the dispatch gates; no standardized end-to-end token-throughput table was reported.",assessment:"High practical value for supported mobile GPUs and a good example of compiler-generation-specific dispatch. The 20% versus 25% wording should be treated as an approximate author result, and gains do not apply to q8-attention variants or decode. Worth reproducing on the exact SoC/driver stack.",citations:[{label:"Merged PR and profiling notes",url:"https://github.com/ggml-org/llama.cpp/pull/27640"}]},
{title:"vLLM: NIXL prefill/decode transfer support for MLA under DCP",url:"https://github.com/vllm-project/vllm/pull/50611",tag:"Disaggregation · vLLM",source:"vLLM pull request #50611 · merged",sourceDate:"2026-08-29",takeaway:"vLLM now routes MLA KV blocks correctly between prefill and decode workers when DCP shards the cache, enabling NIXL disaggregation across replicated and fully sharded layouts.",problem:"NIXL prefill/decode transfer assumed MLA KV was replicated across tensor-parallel ranks, but DCP assigns cache blocks to ranks; a decode rank may therefore need slices from multiple producer ranks.",why:"Disaggregated serving cannot combine MLA, DCP, and NIXL safely unless each consumer reads the exact remote shards it owns; incorrect rank mapping can corrupt cache state or make configurations unusable.",existing:"The replicated-cache path selected one canonical remote rank and did not model block-sharded ownership. A fully general partial-overlap solver would require more complex range and rank intersection logic.",solution:"The merged design maps remote ranks to local DCP ownership for three cases: both sides replicated, one replicated and one sharded, or both fully sharded. KV interleaving is pinned to block size when a connector is active.",results:"DeepSeek-V2-Lite-Chat, GSM8K, block size 64: seven prefill/decode configurations spanning TP2/TP4 and DCP1/DCP2/DCP4 passed, with reported strict-match scores from 0.64898 to 0.66490. No transfer-bandwidth, TTFT, throughput, or latency benchmark was reported.",assessment:"Important enabling work for MLA disaggregation, but the simplifying contract excludes partial DCP degrees where 1 < DCP < TP and assumes the two sides’ DCP sizes divide each other. Worth deep reading for KV-connector implementers; performance impact remains unquantified.",citations:[{label:"Merged PR, rank-mapping diagram, and evaluation sweep",url:"https://github.com/vllm-project/vllm/pull/50611"}]},
{title:"vLLM: FlashInfer MLA support for DSpark drafting under DCP",url:"https://github.com/vllm-project/vllm/pull/54277",tag:"Speculative · vLLM",source:"vLLM pull request #54277 · merged",sourceDate:"2026-08-29",takeaway:"vLLM fixes draft-specific DCP metadata and cache grouping so FlashInfer MLA can support non-causal DSpark drafting without mixing target and draft sequence geometry.",problem:"Speculative draft models advance and rewind sequence lengths differently from the target, while DSpark flattens multi-token query blocks into independent rows; reusing target-local lengths produced mismatched DCP batch geometry.",why:"Wrong draft metadata can break or silently mis-handle speculative MLA attention, and merging causal target layers with non-causal draft layers can apply the wrong cache semantics to both.",existing:"The shared metadata path reused target-local DCP lengths, DFlash carried duplicate preparation, and cache-group merging used any(non_causal_multi_token_decode), which could promote a causal group to draft behavior.",solution:"Recompute rank-local lengths from the draft model’s current global lengths, share the preparation across EAGLE/MTP/DFlash/DSpark, keep causal and non-causal MLA layers in separate cache groups, and repeat causal boundaries to match flattened DSpark rows.",results:"Twelve focused B300 unit tests for FlashInfer MLA DCP metadata/backend behavior passed. End-to-end Kimi-K3 DSpark+DCP evaluation and any latency, acceptance-rate, or throughput measurements were explicitly still pending in the primary source.",assessment:"Genuine correctness and enablement value for an advanced combination of speculative decoding, MLA, DCP, and FlashInfer. Because end-to-end validation was pending, treat it as infrastructure landing rather than a performance result. Worth reading if operating Kimi-K3-like stacks.",citations:[{label:"Merged PR and metadata rationale",url:"https://github.com/vllm-project/vllm/pull/54277"}]},
{title:"SGLang HiCache: Page-aligned chunked CUDA host registration",url:"https://github.com/sgl-project/sglang/pull/36798",tag:"KV Memory · SGLang",source:"SGLang pull request #36798 · merged",sourceDate:"2026-08-29",takeaway:"SGLang now registers huge HiCache host pools in page-aligned chunks, avoiding oversized registration failures and batch-copy boundaries that can trigger invalid-argument errors.",problem:"Very large host KV pools can exceed a practical single cudaHostRegister call, but arbitrary chunk boundaries can split one logical copy page across registrations and make cudaMemcpyBatchAsync fail.",why:"Hierarchical KV caching relies on large, pinned host tiers; registration and teardown must remain correct across MHA, MLA, Mamba, DSA, and asymmetric layouts before bandwidth optimizations matter.",existing:"One monolithic registration does not scale to very large pools, while byte-based chunking ignores logical KV copy-page boundaries and can leave partial registrations behind after failure.",solution:"Register configurable chunks, defaulting to 256 GiB, rounded down to a multiple of each pool’s logical copy-page size; track every successful range, roll back in reverse order on failure, and unregister all chunks on teardown.",results:"The primary source reports 11 chunk-registration tests plus 12 subtests, 51 related host-pool tests with 19 skips plus four subtests, 23 DCP/general host-pool tests under a compatible allocator harness, and three allocator-fallback tests. No throughput, transfer bandwidth, or latency benchmark was reported.",assessment:"Niche but high-value reliability work for extremely large host cache tiers. It does not make copies faster; it removes scale and correctness hazards. Worth reading for HiCache and pinned-memory pool maintainers, not as a standalone performance optimization.",citations:[{label:"Merged PR and validation details",url:"https://github.com/sgl-project/sglang/pull/36798"}]},
{title:"ARCQuant: Boosting NVFP4 Quantization with Augmented Residual Channels for LLMs",url:"https://arxiv.org/abs/2601.07475",tag:"Weights · NVFP4",source:"arXiv:2601.07475 · ACL 2026",sourceDate:"2026-07-04",takeaway:"ARCQuant keeps a unified NVFP4 execution format while injecting quantized residual channels into GEMM, recovering accuracy without mixed-precision kernels.",problem:"NVFP4’s tiny dynamic range makes existing rotation, smoothing, and mixed-precision PTQ strategies either distort fine-grained blocks or violate efficient unified-precision execution.",why:"If NVFP4 can approach FP16 quality while using standard GEMM paths, weight and activation memory bandwidth can fall substantially on supporting GPUs.",existing:"Rotation breaks block isolation, smoothing cannot absorb severe 4-bit error, and outlier channels or mixed precision complicate hardware-efficient kernels.",solution:"Augment the activation matrix with quantized residual channels, folding error compensation into the GEMM reduction dimension; a dual-stage quantizer provides an MXFP8-like worst-case bound.",results:"Experiments on LLaMA and Qwen report near-full-precision perplexity and downstream accuracy; RTX 5090 and RTX PRO 6000 deployment reports up to 3× speedup over FP16. Treat the headline as model-, shape-, and kernel-dependent.",assessment:"Strong algorithm–kernel co-design and directly relevant to NVFP4 serving. The practical question is whether residual-channel overhead remains small across larger models and production batch shapes.",citations:[{label:"Paper and abstract",url:"https://arxiv.org/abs/2601.07475"}]},
{title:"KeepKV: Achieving Periodic Lossless KV Cache Compression for Efficient LLM Inference",url:"https://arxiv.org/abs/2504.09936",tag:"KV Cache · Lossless",source:"arXiv:2504.09936",sourceDate:"2025-11-27",takeaway:"KeepKV merges KV entries while recording merge history and correcting attention, making one-step compression lossless and bounding error across repeated compression.",problem:"Eviction loses context and can cause hallucinations; naïve KV merging changes attention distributions and degrades generation quality.",why:"A compressor that preserves attention behavior under a 10% cache budget could reduce memory pressure without paying the usual quality cost.",existing:"Eviction discards information, while prior merging methods lack a mechanism to compensate for accumulated attention-score inconsistencies.",solution:"Electoral Votes records which entries contributed to merged tokens, and Zero Inference-Perturbation Merging compensates the attention loss; the method supports periodic multi-step compression with error bounds.",results:"The paper reports substantially reduced memory use, more than 2× inference throughput, and retained generation quality with only 10% of the KV budget across multiple models and benchmarks.",assessment:"Conceptually important for retention-oriented KV compression. Verify the lossless claim under the exact merge schedule, kernel implementation, and long-running interactive workload; the abstract does not expose all benchmark details.",citations:[{label:"Paper and abstract",url:"https://arxiv.org/abs/2504.09936"}]},
{title:"ECHO: Elastic Speculative Decoding with Sparse Gating for High-Concurrency Scenarios",url:"https://arxiv.org/abs/2604.09603",tag:"Speculative · Scheduling",source:"arXiv:2604.09603",sourceDate:"2026-05-14",takeaway:"ECHO treats speculative decoding as a budgeted scheduling problem, dynamically trading proposal-tree width against depth for compute-bound high-concurrency serving.",problem:"At high concurrency, verification compute dominates: static trees waste verification while dynamic trees accumulate prediction errors and often fit poorly with optimized kernels.",why:"Speculative decoding only helps production when accepted tokens reduce global verification work without making each verification step inefficient.",existing:"Static trees over-provision branches; per-request dynamic trees create noisy decisions and kernel fragmentation.",solution:"Sparse confidence gating forms a unified super-tree and elastically reallocates budget between depth and width, integrated into SGLang.",results:"Across model scales, including Qwen3-235B, the paper reports up to 5.35× wall-time speedup and over 20% relative speedup versus evaluated baselines across low- and high-load settings.",assessment:"A useful shift from drafter-only tuning toward scheduler/kernel co-design. Reproduction should include acceptance rate, verification FLOPs, batch shape, and tail latency—not only wall-time speedup.",citations:[{label:"Paper and abstract",url:"https://arxiv.org/abs/2604.09603"}]},
{title:"FlashPrefill V2: Block-Sparse Prefill Attention for Long-Context LLM Serving",url:"https://arxiv.org/abs/2608.19758",tag:"Prefill · Sparse Attention",source:"arXiv:2608.19758",sourceDate:"2026-08-20",takeaway:"FlashPrefill V2 turns dynamic block-sparse prefill into a serving backend with mean correction, production GPU techniques, FP8, paged KV cache, and continuous batching.",problem:"Long-context prefill remains quadratic and compute-heavy; the earlier prototype found sparsity but lacked error control and runtime integration.",why:"Prefill attention can dominate TTFT and cluster cost for long prompts, especially when dense attention computes many low-value blocks.",existing:"Dense FlashAttention pays quadratic work; prototype sparsity methods can lose quality, lack paged-cache support, or fail to map onto warp-specialized kernels.",solution:"A mean-correction term controls approximation error, while PackGQA, warp specialization, ping-pong pipelining, FP8, paged KV cache, and continuous batching align the operator with modern runtimes.",results:"On NVIDIA H20 at 128K context, the paper reports up to 47.26× and 27.19× over FA2 for FP8 and BF16, and 30.49× over an FA3/4-aligned dense FP8 baseline. These are unusually large, highly configuration-bound speedups requiring careful reproduction.",assessment:"Potentially high impact for long-context prefill, with unusually strong reported gains. The key diligence items are approximation quality at extreme sparsity, dense-baseline fairness, and behavior under mixed request lengths.",citations:[{label:"Paper and abstract",url:"https://arxiv.org/abs/2608.19758"}]}
]},{date:"2026-08-29",label:"29 Aug 2026",summary:["KV-cache compression is moving from uniform heuristics toward page-local structure, retention-aware formats, and task-aware value preservation.","Tensor parallelism remains the necessary capacity lever when model weights do not fit, while KV compression is often cheaper when the model already fits.","Mixed-format paged attention and page-wise low-rank decomposition increasingly align compression with serving-runtime data structures.","Ultra-low-bit weight work is shifting toward algorithm–kernel co-design and reusable multi-precision representations."],items:[
{title:"Exploring Speculative Decoding in vLLM on AMD GPUs",url:"https://vllm.ai/blog/2026-08-23-speculative-decoding-amd-gpus",tag:"Serving · vLLM",takeaway:"Speculative decoding can reduce target-model work on AMD GPUs, but gains depend strongly on the drafter, proposal length, model, workload, and acceptance behavior.",problem:"Autoregressive decode produces one token per target-model step, leaving generation latency and output throughput constrained.",why:"A successful draft lets one target pass verify several tokens, potentially improving TPOT and output-token throughput without changing the target model.",existing:"Small draft models, n-gram proposals, MTP, Medusa-style heads, and EAGLE-family feature-level drafters trade draft cost against acceptance quality.",solution:"The vLLM evaluation compares speculative-decoding methods and proposal lengths on AMD GPUs, emphasizing acceptance-aware configuration rather than one universal setup.",results:"The primary post reports workload-dependent results; there is no single speedup that generalizes across configurations. Interpret measurements with the exact model family, draft checkpoint, proposal length, request shape, and acceptance rate.",assessment:"High practical value for AMD deployments, but reproduce on the intended traffic distribution. A drafter that looks fast in isolation can lose end-to-end when acceptance is weak or draft overhead is high."},
{title:"Large-Scale Sharded Weight Transfer with Ray Direct Transport",url:"https://vllm.ai/blog/2026-08-22-rdt-weight-transfer",tag:"Systems · vLLM",takeaway:"A Ray-native sharded transfer path reduces the cost of synchronizing large model weights for online RL and distributed inference.",problem:"Online RL periodically synchronizes updated training weights to rollout engines; trillion-scale models make transfer time and temporary memory expensive.",why:"Slow synchronization leaves expensive rollout GPUs idle and can generate samples from stale policy weights.",existing:"Centralized gathering, generic object-store paths, or external transfer services add extra copies, coordination, memory pressure, or operational dependencies.",solution:"vLLM shards the transfer across workers and uses Ray Direct Transport to move weight tensors through a Ray-native direct data path.",results:"The post reports Kimi K2 BF16 weight transfer across 48 nodes with 8 H100 GPUs each in 7.53 seconds. This result is topology- and configuration-specific and should not be generalized to other clusters without reproduction.",assessment:"Strong production relevance for large online-RL clusters. Evaluate transfer time, peak memory, synchronization barriers, failure recovery, and overlap with training/rollout on the target fabric."},
{title:"FlashInfer: Efficient and Customizable Attention Engine",url:"https://arxiv.org/abs/2501.01005",tag:"Kernels · Paper",takeaway:"FlashInfer uses block-sparse representations and customizable JIT templates to cover heterogeneous attention and KV-cache layouts.",problem:"Serving systems need paged, ragged, tree-mask, sparse, prefill, append, and decode attention variants; separate kernels multiply maintenance and tuning cost.",why:"Attention flexibility determines whether serving systems can support prefix sharing, speculative decoding, long context, and dynamic batching without losing efficiency.",existing:"Fixed FlashAttention-style kernels are fast for supported layouts, while general frameworks offer flexibility but may add abstraction or compilation overhead.",solution:"FlashInfer expresses KV-cache layouts with composable block-sparse formats and generates specialized attention kernels through customizable templates and JIT compilation.",results:"The paper evaluates kernel-level and end-to-end serving performance across multiple workloads. Results depend on GPU, layout, sequence length, batch shape, and baseline; consult the paper tables for like-for-like comparisons.",assessment:"Worth deep reading for kernel-library and serving-backend design. Reproduce compile-cache behavior, integration overhead, and the exact prefill/decode shapes used by the target system."}
,{title:"More GPUs or a Smaller Cache? Tensor Parallelism versus KV Compression for Memory-Bound LLM Serving",url:"https://arxiv.org/abs/2608.23962",tag:"Serving · KV Cache",takeaway:"Compression is the cheaper capacity lever when weights fit; tensor parallelism becomes mandatory when model weights exceed one device and remains the latency lever.",problem:"Deployments that exhaust KV-cache memory can add GPUs through tensor parallelism or reduce cache precision/retention, but these choices are rarely compared on one cost axis.",why:"Choosing only by memory relief can waste GPUs or overlook that model weights—not KV cache—are the binding resource.",existing:"Tensor parallelism buys headroom but adds all-reduce and hardware cost; KV compression saves memory but can increase latency and affect quality.",solution:"The paper cost-normalizes TP degrees 1–8 and 16/8/4-bit KV configurations with retention ratios down to 0.25 using profiles from A100, A40, and H100.",results:"Compression is reported as 1.20–2.00× cheaper and up to 16.5× capacity per dollar, while compression increases per-token latency by 8–93%. The reported 80 GB boundary is roughly 36B parameters.",assessment:"A useful deployment framing rather than a new compressor. Validate simulator assumptions, pricing, workload shape, and the exact compression method before using the crossover as a capacity plan."}
,{title:"Beyond Sparse Weights: When Is Attention Compressible? (CertKV)",url:"https://arxiv.org/abs/2608.21541",tag:"KV Cache · Analysis",takeaway:"A sparse-looking attention map is not enough: CertKV preserves a tail summary and allocates the remaining budget using value dispersion.",problem:"Large attention weights may not contain most mass, omitted values can cancel, and matching one attention output does not guarantee task preservation for future queries.",why:"Many eviction methods infer cache compressibility directly from saliency maps, conflating score concentration, output approximation, and task accuracy.",existing:"Top-k retention, windows, sinks, and fixed per-head budgets can discard distributed value information even when maps look sparse.",solution:"CertKV is training-free, reserving one tail-summary slot per head and using value dispersion to allocate the rest of the budget.",results:"It is top-two in 7/9 LongBench-v2 settings, remains competitive on 128K RULER, and demonstrates a ten-fold cache budget in a packed Llama prototype.",assessment:"The taxonomy is highly reusable for evaluating KV compression claims. The evidence supports competitive quality, not universal guarantees across workloads and future-query distributions."}
,{title:"PuzzleKV: Page-Wise Low-Rank Decomposition for KV Cache Compression",url:"https://arxiv.org/abs/2608.23843",tag:"KV Cache · Low Rank",takeaway:"PuzzleKV compresses each completed logical page independently, exploiting local low-rank structure without training or calibration.",problem:"Global or fixed projection spaces may blur distinct cache regions and miss important local directions in long-context KV storage.",why:"Page-local structure aligns naturally with paged attention and can improve the quality-storage tradeoff over one shared global basis.",existing:"Weight-derived spaces, calibration-based spaces, and global SVD are simple but may fail to capture local structure.",solution:"Per layer and KV head, completed pages are factorized independently; attention runs over dense and factorized pages, with incremental compression during decoding.",results:"At about 60% of original storage it reports over 96% of Full-KV performance, gains over Global SVD on RULER, and over 93% performance at 18.7% storage when combined with quantization.",assessment:"Strong systems fit. The key production test is whether factorization, metadata, and mixed kernels amortize under realistic batch sizes and page lengths."}
,{title:"Minima-KV: Retention-Preserving KV Cache Compression with Mixed-Format Paged Attention",url:"https://arxiv.org/abs/2608.23834",tag:"KV Cache · Mixed Format",takeaway:"Minima-KV keeps recent and protected pages in FP8, packs older pages into TQ3, and decodes directly across heterogeneous formats without a dense shadow cache.",problem:"Long-context serving needs lower KV capacity and bandwidth without evicting live-request pages or materializing a second dense representation.",why:"Uniform precision wastes bits on older state, while eviction and dense materialization respectively risk quality and memory savings.",existing:"Uniform FP8, eviction, and decompress-then-attend approaches either spend unnecessary bits, remove information, or add temporary memory traffic.",solution:"Format-specific kernels compute partial attention states and merge them through globally normalized online softmax, preserving page addressability.",results:"On Qwen3.6-27B and one 96 GB RTX PRO 6000 Blackwell, it reports 3.50× compression versus BF16, LongBench deltas of -0.80/-0.60/-0.40 points at 16K/32K/64K, and 0.9821× control throughput in a long-context canary.",assessment:"Especially relevant to runtime engineers. Results are configuration-bound; test portability, scheduling, and quality beyond the reported model/GPU profile."}
,{title:"Benchmarking Composable Compression Techniques in Mixture-of-Experts LLMs (MoEXBench)",url:"https://arxiv.org/abs/2608.21693",tag:"MoE · Benchmark",takeaway:"Expert pruning, weight quantization, and KV compression interact nonlinearly, so standalone compression ratios cannot predict deployment quality or speed.",problem:"MoE models combine large expert footprints, routing imbalance, and long-context KV growth; practical deployments stack multiple compression methods.",why:"A pipeline can amplify degradation or hide architecture-specific failures even when each component looks safe in isolation.",existing:"Most studies evaluate pruning, quantization, or KV compression independently and report average quality.",solution:"MoEXBench evaluates 10 MoE models from 30B–235B across attention architectures, 20–50% pruning, 1–16-bit weights, multiple KV precisions, and eight quality/robustness/deployment modules.",results:"It finds non-trivial interactions, expert pruning as the dominant degradation source, and weak correlation between compression rate and either quality loss or runtime gain.",assessment:"High practical value as an evaluation protocol. It supports selecting compression as a pipeline, not multiplying isolated headline ratios."}
,{title:"SchurQuant: Groupwise Discrete Optimization for Layer-Wise LLM Quantization",url:"https://arxiv.org/abs/2608.15567",tag:"Weights · PTQ",takeaway:"SchurQuant improves ultra-low-bit PTQ by optimizing discrete codes with conditional curvature and task-aware teacher signals.",problem:"Accuracy often collapses at 2–3 bits; fixed-grid discrete refinements ignore compensation from the remaining continuous weights.",why:"Better 2-bit weight-only quantization can determine whether large models fit on commodity accelerators.",existing:"GPTQ-style objectives and fixed affine grids can leave accuracy on the table at ultra-low precision.",solution:"SCHUROPT eliminates the optimal continuous suffix response using a Schur-complement quadratic, then alternates scale/zero-point refitting with integer coordinate descent; SCHURQUANT adds teacher-guided reconstruction and regularization.",results:"The paper reports +11.88 points on 2-bit Qwen3-4B in its GPTQ-objective comparison and +9.65 points over the strongest evaluated backpropagation-free baseline at 2 bits.",assessment:"The conditional-curvature idea is the key contribution. Production adoption still depends on calibration cost, kernel support, and behavior beyond the tested Llama/Qwen models."}
,{title:"FluxBin: Flexible LUT-based Ultra-low-bit LLM Inference by Algorithm-Kernel Synergy",url:"https://arxiv.org/abs/2608.15602",tag:"Weights · CUDA",takeaway:"FluxBin co-designs binary decomposition with CUDA LUT execution to avoid the arithmetic and dequantization overhead that often erases ultra-low-bit gains.",problem:"Binary quantization can compress strongly in theory, but generic kernels may reintroduce floating-point work or irregular execution overhead.",why:"Algorithmic compression only becomes serving speed when the representation maps efficiently to real hardware.",existing:"Binary methods may preserve accuracy but fail to exploit dense GPU execution or avoid runtime dequantization.",solution:"It combines decoupled row-column binary decomposition, Hessian-guided salient bases, LUT construction with scale fusion, and virtual columnar mapping.",results:"The paper reports up to 5.92× speedup, 10.19× energy savings, and 70B deployment on one A100 with 4× memory reduction.",assessment:"A strong algorithm-kernel co-design case study. Recheck gains against the exact baseline kernels, GPU generation, batch/sequence shapes, and preprocessing accounting."}
,{title:"Recurrent Residual Quantization: A Progressive Multi-Precision Representation for LLMs (RRQ)",url:"https://arxiv.org/abs/2608.04048",tag:"Weights · Multi-precision",takeaway:"RRQ packages a low-bit base plus successive residuals so one checkpoint can expose 2-, 4-, 6-, and 8-bit prefixes.",problem:"Conventional PTQ generally needs a separate checkpoint for every target precision, complicating distribution across heterogeneous memory tiers.",why:"A progressive artifact can let serving systems trade accuracy for memory without switching model files.",existing:"Nested bit slicing couples precisions to one hierarchy; direct fixed-bit quantizers optimize one target but do not provide progressive reuse.",solution:"Starting from a 2-bit base, RRQ adds quantized residual corrections, usually with RTN, without Hessian estimation or joint multi-bit optimization.",results:"A Qwen3-8B all-RTN package takes 1,293 seconds, reported as 3.3× faster than measured MatGPTQ construction; 6/8-bit results are competitive, while 4-bit behavior is model-dependent.",assessment:"Its packaging flexibility is the main value. Validate residual application overhead, 4-bit quality, and outlier distribution on the deployment model."}
]}];
function ItemCard({item,index,issueDate}:{item:Item;index:number;issueDate:string}){const citations=item.citations??[{label:"Primary source",url:item.url}];return <article className="item"><span className="itemNo">{String(index+1).padStart(2,"0")}</span><div><p className="tag">{item.tag}</p><h3>{item.title}</h3><p className="takeaway">{item.takeaway}</p><dl className="analysisGrid"><div><dt>Source</dt><dd><a href={item.url} target="_blank" rel="noreferrer">{item.source??item.tag}</a> · {item.sourceDate??issueDate}</dd></div><div><dt>Problem</dt><dd>{item.problem}</dd></div><div><dt>Why it matters</dt><dd>{item.why}</dd></div><div><dt>Existing approaches / limitations</dt><dd>{item.existing}</dd></div><div><dt>Proposed solution</dt><dd>{item.solution}</dd></div><div><dt>Results / benchmark context</dt><dd>{item.results}</dd></div><div><dt>Assessment</dt><dd>{item.assessment}</dd></div><div><dt>Citations</dt><dd>{citations.map((citation,i)=><span key={citation.url}>{i>0?" · ":""}<a href={citation.url} target="_blank" rel="noreferrer">{citation.label}</a></span>)}</dd></div></dl></div></article>}
function Issue({issue}:{issue:Issue}){return <section className="issue" id={issue.date}><div className="issueHead"><div><time dateTime={issue.date}>{issue.date}</time><h2>{issue.label}</h2></div><span>Daily digest</span></div><div className="summary"><h3>Daily Summary</h3><ul>{issue.summary.map(bullet=><li key={bullet}>{bullet}</li>)}</ul></div><div className="articlesHead"><h3>Articles</h3><span>{String(issue.items.length).padStart(2,"0")} items</span></div><div className="items">{issue.items.map((it,i)=><ItemCard item={it} index={i} issueDate={issue.date} key={it.title}/>)}</div></section>}
export default function Home(){return <main><header className="masthead"><a className="brand" href="#top"><span className="brandMark">L·I</span><span>LLM Inference Daily</span></a><div className="edition"><span className="liveDot"/>Daily · 09:00 JST</div></header><div className="layout"><div><section className="hero" id="top"><p className="eyebrow">Independent engineering brief</p><h1>The signal in<br/>LLM inference.</h1><p className="dek">High-signal papers, repositories, pull requests and systems work—filtered for engineers who build and optimize inference stacks.</p></section>{issues.slice(0,7).map(issue=><Issue issue={issue} key={issue.date}/>)}</div><aside className="archive"><details open><summary>Archive</summary><p className="archiveNote">Recent 7 days appear on the home page.</p><nav>{issues.map(issue=><a href={"#"+issue.date} key={issue.date}>{issue.label}<span>→</span></a>)}</nav></details></aside></div><footer><div><span className="brandMark small">L·I</span><b>LLM Inference Daily</b></div><p>Primary sources first · English only</p><p>Updated daily</p></footer></main>}
